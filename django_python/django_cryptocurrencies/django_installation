# pip install django 

2. Create a new Django project: Next, create a new Django project by running the following command:
# django-admin startproject cryptoprices 
 
3. Install required libraries: Inside the cryptoprices directory, run the following commands to install the required libraries:

# pip install python-socketIO socketIOClient 

4. Create a new Django app: Create a new Django app inside the cryptoprices project directory by running the following command:
 
# python manage.py startapp prices 

5. Create database tables: Run the following command to create the database tables for the defined models:

# python manage.py migrate